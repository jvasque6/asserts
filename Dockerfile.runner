FROM alpine:latest

RUN apk update \
    && apk upgrade \
    && apk add python \
    py3-pip \
    python3-dev \
    gcc \
    libc-dev \
    libffi-dev \
    openssl-dev \
    make \
    jpeg-dev \
    curl

RUN pip3 install fluidasserts
