# para que esta configuracion sea cargada esta variable de estar establecida 
# export ANSIBLE_CONFIG=$HOME/git/asserts/test/setup/ansible.cfg
# si se ejecuta desde tox no es necesario definirla pues tox.ini ya la tiene
# inventory: no definir nunca aqui, esta definido en tox.ini:ANSIBLE_HOSTS

[defaults]
retry_files_enabled = False
callback_whitelist  = profile_tasks

[ssh_connection]
pipelining = True
